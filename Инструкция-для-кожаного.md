# 🧑‍💻 Инструкция для кожаного

_Пошаговая методичка для белкового существа: как руками довести три подзадачи
плана до готовности, не потеряв контекст и не наступив себе на ногу._

Связанные файлы (не читать обязательно, но знать, что они есть):
- **`PLAN.md`** — сам план (фазы 0–6, три подзадачи, триггер-промпты). Источник правды.
- **`REVIEW-2026-07-09.md`** — что было не так + `## Журнал выполнения`, куда пишутся отчёты.
- **`CLAUDE.md`** — текущее состояние проекта.

---

## Главный принцип (прочитай один раз)

- **Каждая из 3 подзадач — в НОВОМ чате.** Да, ты понял правильно. Новый чат = чистый
  контекст. Модель поднимает всё, что нужно, из файлов репозитория (`PLAN.md`, `REVIEW`,
  `CLAUDE.md`, код), а не из памяти прошлого чата. Поэтому «забыть» ей нечего.
- **Порядок строгий: 1 → 2 → 3.** Не начинай следующую, пока предыдущая не завершена и
  тесты не зелёные.
- **Не смешивай подзадачи в одном чате.** Один чат = одна подзадача. Так меньше шансов,
  что модель «поплывёт» на длинном контексте.

---

## Разовая подготовка (30 секунд, до Подзадачи 1)

Зафиксируй стартовую точку в git, чтобы было куда откатиться:

```
git add -A
git commit -m "docs: план и инструкции перед фронтенд-доработками"
```

(Если хочешь, чтобы это сделал я в текущем чате — просто скажи. Иначе выполни руками.)

---

## Подзадача 1 — «Сетка + лексер»

1. **Открой новый чат.**
2. Режим: **auto (accept edits)**, не plan.
3. Модель: **Fable 5** (`/model fable`).
4. Вставь ровно это:

   > Выполни Подзадачу 1 (фазы 0–1) из плана `PLAN.md` в корне репозитория.
   > Сначала прочитай `PLAN.md`, `REVIEW-2026-07-09.md`, `CLAUDE.md` и код в
   > `src/`. Не выходи за рамки подзадачи. Финальным шагом внеси отчёт в
   > `REVIEW-2026-07-09.md` по формату из раздела «Формат отчётности» плана.

5. Дождись окончания. **Проверь приёмку** (см. блок «Как понять, что готово» ниже).
6. Если всё зелёно — **закоммить**:
   ```
   git add -A
   git commit -m "Подзадача 1: тесты + устойчивость лексера"
   ```
7. Закрой этот чат.

---

## Подзадача 2 — «Фундамент выражений и присваивания»

1. **Новый чат.** auto-режим. `/model fable`.
2. Вставь:

   > Выполни Подзадачу 2 (фазы 2–3) из плана `PLAN.md` в корне репозитория.
   > Сначала прочитай `PLAN.md`, `REVIEW-2026-07-09.md`, `CLAUDE.md` и код в
   > `src/`. Предыдущая подзадача завершена. Не выходи за рамки подзадачи.
   > Финальным шагом внеси отчёт в `REVIEW-2026-07-09.md` по формату из раздела
   > «Формат отчётности».

3. ⚠️ **Тут нормально, что golden-эталоны AST перегенерируются** (это самый
   рискованный рефактор — Pratt-парсер). Это не поломка. Но когда будешь смотреть
   приёмку — убедись, что модель в отчёте подтвердила: структура дерева
   (приоритеты/ассоциативность) сохранилась.
4. Проверь приёмку → **закоммить**:
   ```
   git add -A
   git commit -m "Подзадача 2: унарный минус, Pratt-парсер, lvalue, FOR..BY"
   ```
5. Закрой чат.

---

## Подзадача 3 — «Объявления, единицы, семантика»

1. **Новый чат.** auto-режим. `/model fable`.
2. Вставь:

   > Выполни Подзадачу 3 (фазы 4–6) из плана `PLAN.md` в корне репозитория.
   > Сначала прочитай `PLAN.md`, `REVIEW-2026-07-09.md`, `CLAUDE.md` и код в
   > `src/`. Предыдущие подзадачи завершены. Не выходи за рамки подзадачи.
   > Финальным шагом внеси отчёт в `REVIEW-2026-07-09.md` по формату из раздела
   > «Формат отчётности».

3. Это самая объёмная подзадача. Если модель сама предложит разбить её на два
   захода (сначала фазы 4+5, потом 6) — соглашайся, это предусмотрено планом.
4. Проверь приёмку → **закоммить**:
   ```
   git add -A
   git commit -m "Подзадача 3: типы-как-данные, VAR-блоки, SourceFile, семантика"
   ```

После этого весь план закрыт. Дальше — Этап 3 (codegen), но это отдельный разговор.

---

## Как понять, что подзадача готова (приёмка)

После каждой подзадачи проверь **сам**, не полагаясь только на слова модели:

1. В терминале:
   ```
   go build ./...
   go vet ./...
   go test ./...
   ```
   Всё должно пройти без ошибок, тесты — зелёные.
2. Прогон примеров всё ещё работает:
   ```
   go run ./src examples/example.st
   ```
3. Открой `REVIEW-2026-07-09.md` → в конце, в `## Журнал выполнения`, должен
   появиться блок про эту подзадачу (дата, модель, что закрыто, результаты тестов).
4. Выше по тексту ревью соответствующие пункты должны быть помечены `✅ [закрыто пN]`.

Если хоть один пункт не выполнен — не коммить и не переходи дальше. Напиши в том же
чате: «go test падает вот с этим: <вставь вывод>, почини в рамках текущей подзадачи».

---

## Если что-то пошло не так

- **Модель полезла за рамки подзадачи** (например, в codegen): останови и напиши
  «выйди из codegen, это не входит в текущую подзадачу, вернись к плану».
- **Тесты красные и не чинятся:** откатись к последнему коммиту
  (`git reset --hard HEAD`) и начни подзадачу заново новым чатом.
- **Сомневаешься, ту ли модель/режим выбрал:** `/model` покажет текущую;
  режим виден в интерфейсе. Нужен auto (accept edits) + Fable 5.

---

## Частые вопросы

**Точно новый чат на каждую подзадачу?**
Да. Это не каприз — так контекст каждый раз свежий и берётся из файлов. Длинный чат на
все три подзадачи начал бы «сжиматься» и терять детали.

**А если я хочу другую модель на какую-то подзадачу?**
Можно. Перед вставкой промпта просто выбери нужную через `/model`. План от модели не
зависит — он весь в файлах.

**Нужно ли мне самому читать PLAN.md?**
Нет. Твоя работа — открыть чат, выбрать режим и модель, вставить промпт, проверить
приёмку, закоммитить. Читать план будет модель.

**Можно ли не коммитить между подзадачами?**
Лучше коммить. Это твои точки отката. Пять секунд работы — а спасают от переделок.
