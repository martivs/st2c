# ST → C Transpiler

Транспилятор: ST (Structured Text, IEC 61131-3) → C. Язык реализации: Go,
только стандартная библиотека. Модуль `st2c`, go 1.22.

Этот файл — **единственный документ проекта** и источник контекста для новых
сессий: состояние, принятые решения, дорожная карта, долги. История доработок
фронтенда (план из 7 фаз и ревью с журналом выполнения) — в git-истории:
файлы `PLAN.md` и `REVIEW-2026-07-09.md` удалены после завершения, поднять их
можно из коммитов до 2026-07-19.

## Команды

```bash
go build ./... && go vet ./...    # обязательно чисто
go test ./...                     # обязательно зелено перед коммитом
go run ./src examples/example.st  # разбор → sema → печать AST
go test ./src/parser -update      # перегенерация golden-эталонов (diff смотреть руками!)
```

## Статус: фронтенд готов, следующий шаг — codegen (Этап 3)

Конвейер: `lexer` → `parser` (строит `ast`) → `sema` → `codegen`.

| Этап | Пакет     | Статус   |
|------|-----------|----------|
| 1    | `lexer`   | готов    |
| 2    | `parser`  | готов    |
| 2    | `ast`     | готов    |
| 2.5  | `sema`    | готов    |
| 3    | `codegen` | заглушка |

### MVP-ограничения языка
- Только целочисленные переменные (`INT`); имена других типов разбираются
  (`x : MyType;`), но семантика для них не проверяется
- Конструкции: присваивание, `IF / ELSE`, `FOR ... TO ... [BY ...] DO`
- Арифметика `+ - * /`, сравнения `= <> < > <= >=`, унарный минус, скобки
- Вложенность конструкций — любая (рекурсивный спуск даёт её «бесплатно»)

### Файлы
```
st2c/
├── go.mod                    # module st2c, go 1.22
├── examples/                 # примеры ST-программ: example.st,
│                             # nested_if_in_for.st, nested_for_in_if.st,
│                             # deeply_nested.st
└── src/
    ├── main.go               # точка входа: лексер → парсер → sema → печать AST
    ├── lexer/lexer.go        # + lexer_test.go (table-driven)
    ├── parser/parser.go      # + parser_test.go (golden + негативные)
    │   └── testdata/         # 4 примера *.st + эталоны *.ast
    ├── ast/ast.go
    ├── sema/sema.go          # + sema_test.go (table-driven)
    └── codegen/codegen.go    # заглушка: только `package codegen`
```

## Что реализовано (по пакетам)

**`src/main.go`** — читает `.st`-файл, прогоняет лексер → парсер
(`ParseSourceFile`) → sema (`Check`; все ошибки в stderr, выход с кодом 1),
печатает AST каждого POU через `String()`. Лексер одноразовый (pull-модель),
поэтому вывод потока токенов Этапа 1 остался в git-истории, а не в `main`.

**`src/lexer/lexer.go`**
- `TokenType` (iota): `EOF`, `ILLEGAL`, `IDENT`, `INT_LIT`, операторы
  (`+ - * / > < = <= >= <>`, `:=`), разделители (`: ; , ( )`), 20 ключевых
  слов (включая `BY`, семейство `VAR_INPUT/VAR_OUTPUT/VAR_IN_OUT/VAR_TEMP`,
  квалификаторы `CONSTANT`/`RETAIN`)
- `Token{Type, Literal, Line, Col}` — позиция 1-based, колонки в рунах
  (`lineStart` в лексере, сброс на `\n`)
- `NextToken()` — посимвольный разбор: пробелы/переносы, комментарии,
  двухсимвольные операторы через `peek()`, идентификаторы → ключевые слова
  (case-insensitive), целые литералы
- Комментарии: блочные `(* ... *)` с вложенностью (3-я ред. IEC) и строчные
  `// ...`; незакрытый `(*` → `ILLEGAL`-токен с позицией начала; одиночные
  `(` и `/` остаются `LPAREN`/`SLASH`
- `tokenNames` для ключевых слов строится из `keywords` в `init()` — новое
  слово добавляется в одном месте

**`src/ast/ast.go`** — узлы дерева через интерфейсы:
- Интерфейсы `Node` (`String()`, `Line()`), `Statement`, `Expression`, `POU`;
  маркерные методы (`statementNode()` и т.п.) разделяют категории узлов на
  уровне типов
- Корень — `SourceFile{POUs []POU}`; `POU` пока реализует только `Program` —
  `FUNCTION`/`FUNCTION_BLOCK` лягут рядом без смены корня
- `ast.Op` — собственный enum операций (`ADD … NE`, `NEG`) с `String()`,
  возвращающим исходные символы (`+`, `<=`, `<>`); AST отвязан от
  `lexer.TokenType`
- Узлы: `Program` (`VarBlocks []*VarBlock`); `VarBlock{Kind VarKind,
  Constant, Retain bool, Decls}` — вид блока (`VAR`/`VAR_INPUT`/…) как данные;
  `VarDecl{Names []string, TypeName string, Init Expression}` — список имён
  (`a, b, c : INT;`), тип строкой, необязательный инициализатор
  (`x : INT := 5;`); `AssignStatement` (`Target Expression` — lvalue, пока
  всегда `*Identifier`, позже `IndexExpr`/`MemberExpr`); `IfStatement`;
  `ForStatement` (`Var *Identifier`, `Step Expression`, nil → шаг 1);
  `Identifier`; `IntLiteral`; `BinaryExpr` (арифметика и сравнения — единый
  тип, различаются `Op`); `UnaryExpr` (унарный минус; позже `+`/`NOT`)
- `String()` каждого узла рекурсивно печатает поддерево с отступами (формат
  golden-эталонов); поле `Tok lexer.Token` — якорь позиции

**`src/parser/parser.go`** — recursive descent:
- `New(*lexer.Lexer) *Parser`; `ParseSourceFile() (*ast.SourceFile, error)` —
  единственная точка входа: цикл по POU до EOF, диспетчер по стартовому
  ключевому слову (пока только `PROGRAM`; `FUNCTION`/`FUNCTION_BLOCK` —
  будущие ветки `switch`); мусор на верхнем уровне → ошибка
- Окно из двух токенов (`cur`/`peek`), хелперы `nextToken`/`expect`/`curIs`/`peekIs`
- Правила: `parseProgram`, `parseVarBlocks` (цикл по семейству `VAR*` через
  таблицу `varBlockKinds`, квалификаторы `CONSTANT`/`RETAIN`), `parseVarDecl`
  (список имён через запятую, тип, необязательный `:= init`), `parseType`
  (ключевое слово `INT` или любой `IDENT` — пользовательские типы не падают,
  допустимость проверит sema), `parseStatements`, `parseStatement`,
  `parseAssign` (цель — primary-lvalue, пока только идентификатор), `parseIf`,
  `parseFor` (необязательный `BY step` перед `DO`)
- Выражения — Pratt / precedence climbing: одна `parseExpression(minPrec int)`
  + таблица `prec map[lexer.TokenType]int` (уровни по IEC: `= <>` слабее
  `< > <= >=`, дальше `+ -`, `* /`); левая ассоциативность — правый операнд с
  `pr+1`; `parseUnary` (унарный `-`, рекурсивно) → `parsePrimary` (`IDENT`,
  `INT_LIT`, `( expr )`). **Новый бинарный оператор = по строке в `prec` и
  `binOps`**
- Ошибки — **fail-fast**: поле `err`, `expect()`/`fail()` формируют сообщение
  с номером строки; `ParseSourceFile` возвращает первую ошибку
- `optionalSemicolon()` — необязательный `;` после `END_IF`/`END_FOR`

**`src/sema/sema.go`** — семантический анализ между парсером и codegen:
- `Check(*ast.SourceFile) []error` — свой scope на каждый POU (один уровень;
  стек scope-ов придёт с функциями); ошибки **копятся** (не fail-fast), все с
  позицией `line N:C`
- Символьная таблица `map[string]*Symbol` с ключом `strings.ToUpper(name)` —
  идентификаторы по IEC регистронезависимы (`Sum` = `sum`)
- Проверки: объявленность (ссылки в выражениях, цели присваиваний, переменная
  `FOR`), дубликаты объявлений (в т.ч. в разном регистре и между блоками),
  диапазон `INT` −32768..32767 для литералов при известном целевом типе (для
  пользовательских типов не навязывается); `-32768` (NEG+литерал) учитывает
  знак до проверки диапазона

Пример вывода:
```
go run ./src examples/example.st
# ST source loaded: 214 bytes
#
# Program(Example)
#   VarBlock(VAR)
#     VarDecl(x : INT)
#   ...
#   For(x)
#     ...
#       Assign
#         Target:
#           Ident(sum)
#         Value:
#           Binary(+)
#             Ident(sum)
#             Ident(x)
```

## Тесты

- `go test ./...` — обязательно зелено перед коммитом.
- Лексер: table-driven (вход → список `(Type, Literal)`), регистр ключевых
  слов, комментарии, номера строк и колонок.
- Парсер: golden — `testdata/*.st` разбирается, `SourceFile.String()`
  сверяется с `testdata/*.ast`; эталоны перегенерируются
  `go test ./src/parser -update`, **diff просматривать вручную** (структура
  дерева — приоритеты, ассоциативность — не должна меняться неосознанно).
  Плюс структурные тесты выражений/`FOR BY`/VAR-блоков и негативные: вход →
  подстрока сообщения об ошибке с номером строки.
- Sema: table-driven — вход-программа → ожидаемые подстроки ошибок
  (`line:col`) в порядке следования либо их отсутствие.

## Архитектурные решения (зафиксированы — не переоткрывать)

- **AST остаётся синтаксическим.** Результаты семантики (выведенные типы) —
  side-table `map[ast.Expression]Type` внутри `sema`, не поля узлов.
- **Регистронезависимость** IEC: ключевые слова нормализует лексер,
  переменные — sema (ключ таблицы `ToUpper`); оригинальное написание
  сохраняется для сообщений об ошибках.
- **Парсер fail-fast, sema копит ошибки.** Восстановление после ошибок
  парсера отложено: поле `err` расширяется до `errs []error` без переделки
  архитектуры (synchronize до ближайшего `;`/`END_*`).
- **Контракт codegen** (зафиксировать при реализации):
  `Generate(*ast.SourceFile) (string, error)` (или вариант с `io.Writer`),
  рекурсивный обход по образцу `String()`.
- **Маппинг на C** (решено заранее): `INT` → `int16_t` (`#include
  <stdint.h>`); `=` → `==`, `<>` → `!=`; скобки вокруг каждого `BinaryExpr`
  (надёжность важнее читаемости C-выхода); `FUNCTION` → обычная C-функция;
  `FUNCTION_BLOCK` → `struct` состояния + `void FB_Name(FB_State *self, ...)`;
  `PROGRAM` → struct-состояние + функция шага (задел под циклическое
  исполнение PLC).
- **`FOR ... BY`**: направление цикла при отрицательном шаге учесть в codegen
  (условие `<=` против `>=`).

## Дорожная карта

### Этап 3 — codegen (следующий)
Вход `*ast.SourceFile`, выход — C-код по маппингу выше. Обход рекурсивный:
VAR-блоки → декларации переменных (инициализаторы!), `AssignStatement`,
`IfStatement` → `if/else`, `ForStatement` → `for` (шаг/направление),
выражения → скобки вокруг каждой бинарной операции. Попутно CLI-флаги в
`main.go`: `-o out.c`, полезны `-dump-ast`/`-dump-tokens`.

### Дальше (порядок примерный)
- **Этап 4 — типы**: `TypeSpec` в AST (`ARRAY [1..10] OF INT` и т.п.), вывод
  типов выражений в sema (side-table), преобразования — безопаснее явные
  `*_TO_*`-функции с запретом неявных смешений.
- **Этап 5 — операторы**: `ELSIF / WHILE / REPEAT..UNTIL / CASE / EXIT /
  RETURN` — встают в `parseStatement` и `isBlockEnd` независимо друг от
  друга; восстановление после ошибок парсера.
- **Этап 6 — POU**: тела `FUNCTION` / `FUNCTION_BLOCK` (каркас
  `SourceFile`/`POU` и виды VAR-блоков уже готовы — это новые ветки `switch`);
  делать вместе с их codegen, иначе не проверить сквозным C; стек scope-ов в
  sema.

## Известные долги (сознательно отложены)

- Литерал ключевых слов приводится к верхнему регистру — оригинал теряется
  (важно для будущего форматтера/сорсмапов).
- Числовые литералы — только десятичные целые. Нет `16#FF`, `2#1010`,
  `1_000`, `3.14`, `INT#5`, `T#1s`, строк с `$`-экранированием — добавлять
  вместе с типами, которым они нужны (`readInt` → `readNumber` с
  диспетчеризацией).
- `IntLiteral.Value` — `int`; для литералов вне `int64` и типизированных
  литералов понадобится хранить исходную строку.
- Вызовы функций `f(x, y)` в выражениях: при Pratt `(` после primary — это
  постфиксный оператор, ляжет в ту же таблицу приоритетов.
- `a < b < c` разбирается левоассоциативно без предупреждения (зафиксировано
  тестом); отлов «сравнение BOOL с INT» — при выводе типов.
- Условие `IF` в sema проверяется с целевым типом INT (других типов в MVP
  нет); честный вывод типов (у сравнений — BOOL) — со слоем типов.
- Свёртка констант отсутствует: `--32768` пройдёт проверку диапазона.
- Лексер принимает не-ASCII идентификаторы (`unicode.IsLetter`):
  `переменная : INT;` молча проходит фронтенд, но валидного C из такого имени
  не получить — codegen обязан отвергать их внятной ошибкой (решение 8 в
  `PLAN-2026-08-07.md`); транслитерация отложена.
- Присваивание в `VAR CONSTANT` не диагностируется: `sema.Symbol` не хранит
  вид блока и квалификаторы, запись в константу молча проходит.
- Раскладка `src/*` вместо Go-идиомы `internal/` + `cmd/st2c` — переезд
  отдельным механическим коммитом при появлении CI/внешних импортов.

## Рабочий стиль

- Этапы реализуются строго по одному; переход к следующему — только после
  явного «переходим дальше» от пользователя.
- Каждая законченная порция работы = отдельный коммит: `go build ./...`,
  `go vet ./...`, `go test ./...` чисто/зелено; примеры из `examples/`
  прогоняются.
- Golden-эталоны перегенерируются только осознанно, diff просматривается.
- Этот файл обновляется в том же коммите, который меняет поведение, статусы
  или список файлов.
